import styles from './footer.module.css';

import React from 'react';

const Footer = (props) => {
  return <footer className={styles.footerTitle}>Code your dream</footer>;
};

export default Footer;
